# @wordpress/scripts declares an optional peer on @wordpress/env@^10, but we pin
# @wordpress/env@11. yarn (classic) ignored peer conflicts; npm enforces them, so
# opt back into the lenient behavior to keep installs working with the pinned versions.
legacy-peer-deps=true
